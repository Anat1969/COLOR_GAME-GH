node_modules
dist
*.local
.DS_Store

# כלי בדיקה חזותית — תוצרי בנייה זמניים, לא נשמרים
scripts/shot.mjs
scripts/patterns.mjs
# (scripts/pack.mjs כן נשמר — הוא כלי האריזה של הקובץ ללחיצה)
# הקובץ העצמאי "הרמוניה.html" נכתב לתיקיית ההורה, מחוץ למאגר.
